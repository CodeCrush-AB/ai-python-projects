
name = open("name", "a")
# using w instead of a will overwrite the whole file and also create a file if the name does not exist
#can also be used to create a whole new program file like .py or .html
name.write("\nali")
name.close()
